{
  "name": "simulador-player",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "npm run validate:capsules && npm run validate:game3 && npm run liverun:game3 && npm run validate:game4 && vite build",
    "preview": "vite preview",
    "validate:game3": "node scripts/validate-game3.mjs",
    "liverun:game3": "node scripts/liverun-game3.mjs",
    "validate:capsules": "node scripts/validate-action-capsules.mjs",
    "validate:game4": "node scripts/validate-game4.mjs"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "vite": "^4.4.0"
  }
}
